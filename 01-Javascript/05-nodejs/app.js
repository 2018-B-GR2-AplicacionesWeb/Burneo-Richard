// app.js
const node = require('./nodejs.js');
const runtime = require('./runtime.js');
const so = require('./so.js');
const util = require('./util/util');
const na = requiere('../01-varaibles');
const fs = require ('fs');
const express = require('express');
console.log(express);
console.log(fs)
console.log(node);
console.log(runtime);
console.log(so);
console.log(so.archivos);
console.log(util());

